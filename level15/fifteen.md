# Activity 15: Modal/Overlay Component

**Objective:** Create a modal overlay using positioning and z-index.

**Tasks:**
1. Add `bg-opacity-50` to the first `<div>` element (Modal Overlay) - makes overlay semi-transparent (already has bg-black and fixed inset-0)
2. Add `flex justify-center items-center` to the first `<div>` element (Modal Overlay) - centers the modal
3. Add `rounded-lg shadow-xl max-w-md w-full mx-4` to the second `<div>` element (white modal) - styles the modal
4. Add `z-50` to the first `<div>` element (Modal Overlay) - ensures modal appears above everything
5. The close button (×) is already in the top-right; ensure it has proper styling if needed

**Key Concepts:**
- Fixed positioning for overlays
- Z-index layering
- Centering modals
- Creating backdrop/overlay effects

